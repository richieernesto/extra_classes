class ProductDetails{
String ? thumbnail;
String ? description;
String ? price;
String ? nametutor;
String ? schoolOftutor;


ProductDetails({
  required this.thumbnail,
  required this.description,
   required this.price,
  required this.nametutor,
  required this.schoolOftutor,

});

}